import 'package:flutter/material.dart';

class AndroidNewPost extends StatelessWidget {
  const AndroidNewPost({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
